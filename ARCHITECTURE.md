# read-between Skill 架构报告

## 一、运行逻辑

### 1.1 总流程

```
用户输入
  │
  ├─→ [触发检测] SKILL.md description 关键词匹配
  │     匹配：恋爱/约会/追求/星座/八字/MBTI/聊天分析/分手/复合/...
  │     不匹配：工作沟通/销售话术/非感情的古籍问题 → read-between 不激活
  │
  ├─→ [安全分流闸] ←── 最高优先级，不可绕过
  │     │ 检查：暴力/胁迫/自伤/跟踪/私密影像威胁/未成年人/重大权力差
  │     │ 命中 → safety-abuse-and-crisis.md（停止所有普通分析）
  │     │ 未命中 ↓
  │
  ├─→ [意图识别] ←── intent-insight.md
  │     │ 表层情绪（伤心/愤怒/焦虑/...）
  │     │ 深层情绪（焦虑→需要控制感，过度分析→害怕被拒绝，...）
  │     │ 真实意图分类：
  │     │   · 寻求确认 → 温和给替代解释，不过度争论
  │     │   · 情绪宣泄 → 转 emotional-containment.md，暂停分析
  │     │   · 决策求助 → 转 pursue-or-not.md 或相应框架
  │     │   · 技巧学习 → 转策略/代拟模块
  │     │   · 危机求助 → 转 safety-abuse-and-crisis.md
  │     │ 矛盾信号识别与温和反馈
  │     ↓
  │
  ├─→ [生命周期定位] ←── lifecycle-routing.md
  │     │ 判断当前处于11个阶段中的哪个：
  │     │   1.自我准备  2.初识  3.暧昧  4.约会  5.关系定义
  │     │   6.稳定伴侣  7.同居/婚前  8.婚姻  9.危机修复
  │     │   10.安全旁路  11.关系结束
  │     │ 每个阶段有：信号/任务/允许行动/需同意行动/风险/停止条件/转换条件
  │     ↓
  │
  ├─→ [任务路由] ←── 加载对应的参考文件
  │     │
  │     ├─ 追不追犹豫 → pursue-or-not.md（五步决策框架）
  │     ├─ 怎么追 → pursuit-strategy.md（四阶段策略）
  │     ├─ 追到之后 → early-dating.md（约会→关系建立）
  │     ├─ 聊天分析 → chat-ingestion-contract.md + chat-evidence-and-uncertainty.md
  │     ├─ 消息代拟 → reply-drafting.md
  │     ├─ 情绪崩溃 → emotional-containment.md
  │     ├─ 冲突处理 → conflict-and-repair.md
  │     ├─ 关系教学 → relationship-education.md
  │     ├─ 星座/MBTI/八字 → personality-insight-layer.md
  │     ├─ 古典视角 → classical-interpretation.md → classical-catalog.md → classical-voices.md → classical-corpus/
  │     └─ 婚后问题 → partnership-marriage-overview.md（精简版）
  │     ↓
  │
  ├─→ [输出契约] ←── output-contracts.md
  │     │ 四种输出模式：
  │     │   快速回复：首选成品 + 理由 + 三个后续分支
  │     │   聊天分析：原话 + 替代解释 + 未知 + 证据等级 + 验证动作
  │     │   训练复盘：原句 + 有效处 + 风险处 + 替换表达 + 练习目标
  │     │   高风险支持：安全→资源→暂停
  │     ↓
  │
  └─→ [记忆判断]
        │ 是否出现值得长期保存的里程碑？
        │ 是 → 展示拟保存内容 → 等待用户确认 → 写入 → 重读验证
        │ 否 → 不写档案（默认）
```

### 1.2 关键设计决策

**为什么安全分流是第一步？**
因为在暴力或强制控制场景中，"优化聊天技巧"或"分析对方信号"不仅无用，甚至有害。安全分流必须在所有其他逻辑之前执行，且不可被任何用户请求、风格切换或角色扮演绕过。

**为什么意图识别在分析之前？**
用户说"帮我分析这段聊天"可能意味着：
- 真的需要分析（决策求助）→ 给出聊天分析
- 其实已经有了判断，只是需要确认（寻求确认）→ 温和给替代解释
- 情绪上需要被倾听，不是需要方案（情绪宣泄）→ 进入感性隔离区

如果在不识别意图的情况下直接分析，会导致：给情绪宣泄的人长篇分析（对方听不进去）、给寻求确认的人替代解释（对方抗拒）、给真正需要决策的人模糊安慰（对方更焦虑）。

**为什么星座/八字/MBTI 作为"洞察层"而非"决策层"？**
三个系统都能提供关于人的性格和倾向的有用视角。但它们不能预测具体个体的行为，更不能判定两个人"合不合适"。read-between 将它们放在第1层（参考框架），第2层的关系科学（实际行为观察）始终覆盖第1层的推断。

### 1.3 不可协商规则的执行机制

SKILL.md 列出8条 Non-Negotiable Rules。它们在架构中通过以下机制强制执行：

| 规则 | 执行机制 |
|------|---------|
| 安全优先 | 流程最前端的 safety-abuse-and-crisis.md 分流闸 |
| 不输出无依据结论 | output-contracts.md 证据等级系统 + chat-evidence-and-uncertainty.md 未知标记 |
| 不诊断人格 | source-registry.md 明确 MBTI/星座为参考框架而非诊断工具 |
| 消息不含欺骗与操纵 | reply-drafting.md 质量检查清单（不得包含吃醋/冷暴力/制造危机） |
| 不克隆人格 | dialogue-practice.md 人格克隆禁令 |
| 星座/MBTI 只作参考 | personality-insight-layer.md 每次使用的强制不确定性声明 |
| 聊天默认不存档 | memory-protocol.md 默认"不记忆"模式 |
| 写入须确认 | memory-protocol.md 展示→确认→写入→重读验证流程 |

---

## 二、构建思路

### 2.1 设计原则

**原则1：务实主义——帮助用户做出更好的决定，而非提供"完美的理论"**

read-between 不为验证任何特定的心理学流派而存在。它的唯一目标是：当用户面临关系中的困惑、犹豫和痛苦时，帮助ta看清自己的处境、理解自己的感受、做出清醒的选择。这意味着：
- 每个概念都必须回答"这对我现在有什么用？"
- 拒绝为了完整而塞入无关的教学内容
- 理论是工具，不是目的

**原则2：证据校准——知道什么能说、什么不能说**

关系的许多问题是无法用"确定性"来回答的（"她喜欢我吗？""我们会在一起吗？"）。read-between 的策略不是假装有答案，而是诚实地标注不确定性：
- 证据等级（high/medium/low/insufficient）应用于所有分析
- 任何判断都必须附带"什么会推翻它"
- 在没有数据时，说"我不知道"并解释为什么

**原则3：安全与伦理不可妥协**

不因用户想要"更有效的追求技巧"而教授操控。不因星座/八字显示"很好的匹配"而鼓励忽视实际红旗。不因"古人就是这么做的"而建议接受不平等的对待。

**原则4：用户主权——替用户看清选项，不替用户做决定**

read-between 是参谋，不是指挥官。追/不追决策框架的五步法每一步都在帮用户看清，最后一步明确说"不替用户决定"。消息代拟不塞入用户没想说的话。记忆系统要求确认后才写入。

### 2.2 与原始 plan.md 的关键分歧

原 plan.md（relationship-coach 设计方案）是一个优秀的起点，但read-between 在以下几个核心维度上做了重大调整：

| 维度 | 原 plan | read-between 选择 | 理由 |
|------|---------|-------------|------|
| 星座/八字/MBTI | 明确排除，不纳入 | 深度集成，但设使用边界 | 用户明确要求。与其让用户自己去别处查星座配对然后被误导，不如提供负责任的分析 |
| 重点阶段 | 均匀覆盖全周期 | 追求期（阶段1-5）为重 | 用户需求决定。大多数求助发生在"追不追、怎么追、刚追到"的阶段 |
| 古籍策略 | 通过 past-wis skill 接口调用 | 预筛选30条清单，直接路径读取 | past-wis 的2轮检索对 read-between 的场景过重。感情相关的古籍是有限的、可预筛选的 |
| 婚后内容 | 独立大文件 | 合并为一个精简概要 | 主战场在追求期，婚后是概要级支持 |
| 意图识别 | 未独立成模块 | 独立 intent-insight.md | 这是真实使用中的最大痛点——用户和AI经常"频道不对" |
| 追/不追框架 | 包含在关系判断中 | 独立 pursue-or-not.md（五步法） | 这是用户最核心的决策之一，值得一个完整的结构化框架 |

### 2.3 文件设计的"路由-加载"模式

read-between 采用了与 journal-auditor、master-writing 等现有 skill 一致的模式：

- **SKILL.md 是路由器**：定义触发条件、不可协商规则、参考文件列表和工作流概要。不包含详细的教学内容。
- **references/ 是按需加载的深度指令**：每个文件覆盖一个独立领域，只有在用户问题匹配时才被读取。
- **scripts/ 是确定性验证工具**：PowerShell 脚本负责格式检查、时间标准化、去标识化等不需要模型推理的机械任务。

这样设计的好处：
1. **上下文效率**：不需要每次都把24个参考文件全部载入模型上下文
2. **可维护性**：修改某个领域（如星座模块）不影响其他部分
3. **可测试性**：每个参考文件和脚本可以独立验证
4. **一致性**：与项目中其他 Tier 2 skill 的组织方式一致

---

## 三、架构细节

### 3.1 三层洞察架构

```
┌─────────────────────────────────┐
│  第1层：人格洞察层               │
│  星座 / 八字 / MBTI              │
│  "她可能是怎么感受世界的？"      │
│  提供参考框架，不替代实际观察    │
├─────────────────────────────────┤
│  第2层：关系科学层               │
│  现代心理学 / 沟通理论 / 证据校准 │
│  "实际发生了什么？这意味着什么？" │
│  主导分析和建议                  │
│  实际行为永远覆盖第1层            │
├─────────────────────────────────┤
│  第3层：古籍智慧层               │
│  past-wis 桥接（30条预筛选清单） │
│  "古人对此有过怎样的体察？"      │
│  用户明确要求或场景自然需要时接入 │
│  不覆盖现代平等/同意/安全原则     │
└─────────────────────────────────┘
```

**冲突裁决**：第2层 > 第1层 > 第3层。

例："她的月亮星座是巨蟹，按理说应该很顾家，但实际上她从不主动联系你" → 实际行为（不主动联系）覆盖星座推断。

### 3.2 追/不追决策框架的设计哲学

pursue-or-not.md 的五步法不是"感情决策算法"，而是一套**帮助用户看清自己处境的结构化思维工具**：

- **第一步（信号盘点）** 回答："对方到底有没有意思？"——对抗愿望投射
- **第二步（自我澄清）** 回答："我到底想要什么？"——对抗孤独驱动和虚荣驱动
- **第三步（代价评估）** 回答："追的风险是什么？"——对抗冲动决策
- **第四步（可逆性设计）** 回答："怎么追才能最小化伤害？"——对抗"表白=赌注"思维
- **第五步（决策与行动）** 回答："我选择哪条路？"——但最终决定权在用户

每一步都是一次"看清"的机会。用户可能在任何一步意识到"我不想追了"或"我现在清楚了"——这本身就是有价值的输出。

### 3.3 人格洞察层的边界设计

personality-insight-layer.md 是 read-between 最大的单文件（~400行）。它覆盖星座（太阳/月亮/上升/合盘/过境）、八字（日主/五行/流年）和 MBTI（认知功能栈）。每个系统都使用统一的三段结构：

1. **基本要素**：这个系统是什么、怎么看
2. **在关系中的使用**：可以怎么用它来理解沟通差异和冲突模式
3. **使用边界**：什么情况下不能用它（附具体的禁止用法列表）

每次分析强制附带不确定性声明，且禁止用法列表中包含用户最常见但最危险的用法（如"八字不合建议分手"）。

### 3.4 古典自服务层的设计

read-between 内置了完整的古典智慧层，不依赖外部 skill。包含 10 部中国古典文本（《诗经》《世说新语》《菜根谭》等，共 ~26,000 行），通过以下组件自服务：

- `classical-interpretation.md`（588 行）：古典释义宪法——触发条件、场景路由、五步释义纪律、联网现代参照规则、冲突裁决
- `classical-catalog.md`（182 行）：10 部典籍的导航目录——朝代、体裁、情感场景标签、核心章节、使用说明
- `classical-voices.md`（388 行）：五个古典声音（共情者/直言者/追问者/说书人/烟火人）——语气、节奏、文本偏好、禁忌
- `classical-corpus/`（11 文件，~25,800 行）：10 部古典原文 + 使用说明
- `scripts/search_classics.ps1`：关键词检索脚本——用于在 25,800 行语料中快速定位相关段落

五步释义纪律：原文摘录 → 白话转译 → 情感连接（古人这样感受过 → 你现在的感受）→ 联网现代参照（可选）→ 限度的完整表达（"这不证明什么"）。

证据地位：古典引用始终为第 4 级证据——只作理解或叙事材料，不单独作为决策证据。现代平等、同意、安全原则 > 古籍。

### 3.5 记忆系统的字段级治理

memory-protocol.md 在 plan.md 的基础上增加了三层关键设计：

1. **三种记忆模式**（不记忆/会话/长期档案，默认不记忆）——用户不需要为每次吐槽担心被永久记录
2. **字段级敏感级别**（normal/sensitive/highly-sensitive）——极敏感信息不因"有用"自动建议保存
3. **agent-inference 不能自动升级为 user-confirmed**——模型推断永远是推断，除非用户明确确认

这些设计的共同目标是：**让记忆系统服务于用户，而不是成为一个隐私负债。**

### 3.6 输出契约的四模式设计

output-contracts.md 定义了四种输出模式，分别对应四种不同的用户需求场景：

- **快速回复**适用于"帮我想想要回什么"——用户需要的是可执行的草稿，不是分析
- **聊天分析**适用于"帮我看看这段聊天"——用户需要的是多角度解读，不是一条消息
- **训练复盘**适用于"上次约会/聊天我哪里做得不好"——用户需要的是可操作的改进点
- **高风险支持**适用于安全危机——覆盖所有其他模式，优先安全和现实资源

四种模式的共同基础是 evidence level（high/medium/low/insufficient）——确保任何判断都有依据，任何不确定性都明确标注。

---

## 四、与 GitHub 竞争项目的差异化

research-report.md 审计了42个 GitHub 仓库。read-between 的差异化不是"话术更多"或"覆盖更广"，而是以下5个维度的组合：

1. **安全治理统一**：暴力/强制控制有独立的处置流程，不是关系技巧的"特殊情况"
2. **证据校准**：所有判断带证据等级、可推翻条件和未知标注
3. **非操控承诺**：明确拒绝 PUA/推拉/制造嫉妒/人格克隆，且通过8条不可协商规则强制执行
4. **人格洞察的负责任集成**：星座/八字/MBTI 有明确的使用边界，不预测"合不合适"
5. **记忆的用户主权**：三种模式、字段级治理、展示-确认-写入-重读流程

这些不是"额外功能"——它们是贯穿整个 skill 架构的设计约束。每个参考文件、每个输出模式、每个验证脚本都在执行这些约束。

---

## 五、文件统计

| 类别 | 文件数 | 总行数 |
|------|--------|--------|
| SKILL.md（核心路由） | 1 | 181 |
| agents（界面定义） | 1 | 5 |
| references（参考文件） | 24 | ~3,800 |
| scripts（验证脚本） | 8 | ~1,080 |
| config（配置） | 1 | 1 |
| **总计（不含古典语料）** | **36** | **~5,250** |

注：3个原始文件（plan.md, research-report.md, research-packet.json）保留在原目录中，不计入新建文件。
古典语料（classical-corpus/） | 11 | ~25,800 | 10部经典原文，按需检索

### 参考文件按层分布

- 基础层（3）：output-contracts, source-registry, lifecycle-routing
- 追求核心层（4）：intent-insight, pursue-or-not, pursuit-strategy, early-dating ← 最大文件组
- 聊天功能层（5）：emotional-containment, chat-ingestion-contract, chat-evidence-and-uncertainty, reply-drafting, dialogue-practice
- 关系深度层（3）：relationship-education, conflict-and-repair, partnership-marriage-overview
- 守护层（3）：safety-abuse-and-crisis, privacy-threat-model, memory-protocol
- 桥接层（2）：classical-interpretation + classical-corpus（自服务古典层）, personality-insight-layer
- 古典层（5）：classical-interpretation, classical-catalog, classical-voices, classical-corpus（11文件）, search_classics.ps1
- 配置（1）：data-root.txt
